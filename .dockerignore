# The build context only needs the dependency manifest and its lockfile.
# Everything else is bind-mounted
# at run time, so excluding it keeps the context (and rebuild times) tiny.
*
!pyproject.toml
!uv.lock
